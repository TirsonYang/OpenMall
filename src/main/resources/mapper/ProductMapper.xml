<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ytx.openmall.server.mapper.ProductMapper">


    <insert id="save">
        insert into open_mall.product (product_id, name, description, price, category_id, image, stock, status, create_time, update_time)
        VALUES (#{productId},#{name},#{description},#{price},#{categoryId},#{image},#{stock},#{status},#{createTime},#{updateTime})
    </insert>

</mapper>